Necesito calcular el 
n
n
-ésimo número de la secuencia de Fibonacci, pero para un 
n
n
 muy grande: 
n
=
500
,
000
n=500,000
.
Escribe un script en Python que resuelva esto.
Restricción: No puedes usar recursividad simple ni bucles lentos que tarden demasiado. Debes implementar el algoritmo de Exponenciación de Matrices (Matrix Exponentiation) o Fast Doubling para lograr una complejidad 
O(log n)

.
El programa debe calcular el valor y, dado que el número será gigantesco, imprimir solo los últimos 100 dígitos.

uando termine graba la logica de programacion en un archivo de nombre logicautilizada.md
si hubieron errores explica por que y como lo solucionaste en dicho archivo (logicautilizada.md)
importante tiene que grabar el archivo python no solo ejecutar los commando para que quede evidencia de que realizaste la programacion

ya tienes instalado numpy Version: 2.3.5 no tiene que ejecutar el commando 'pip show numpy'
es importante que no ejecutes el commando 'pip show numpy' pues su salida es demaciado grande