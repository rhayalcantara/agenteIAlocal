Escribe un script en Python que calcule el factorial de 1000 (
1000
!
1000!
). Luego, el programa debe sumar todos los dígitos individuales de ese número resultante y mostrar la suma final. El código debe incluir una función de verificación que use una aproximación matemática (como la fórmula de Stirling o logaritmos) para confirmar que el orden de magnitud del resultado es coherente antes de imprimir la suma de los dígitos.
este script debe ser llamado desde la consola y realizar lo que se le pide, tiene que verificar que funcione cuando termine graba la logica de programacion en un archivo .md
si hubieron errores explica por que y como lo solucionaste en dicho archivo 
importante tiene que grabar el archivo python no solo ejecutar los commando para que quede evidencia de que realizaste la programacion
## Seguridad
 - para comando de cmd tiene que solicitar permiso no puedes ejectuar commando sin permisos